# Data Directory

Place your `prezziario.xml` file here.

The file should be named `prezziario.xml` or update the `XmlFilePath` setting in `appsettings.json` to match your filename.

After placing the XML file, call the `/api/prezziario/initialize` endpoint to load the data into the SQLite database.
